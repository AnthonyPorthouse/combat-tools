// Tests live in useCamera.test.tsx (requires JSX for the CameraProvider wrapper).
// This file cannot be deleted; vitest requires at least one test entry.
import { it } from "vitest";
it.todo("see useCamera.test.tsx");
