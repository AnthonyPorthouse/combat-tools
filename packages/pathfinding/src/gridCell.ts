import type { Vector2 } from "@combat-tools/vectors";

/**
 * A grid cell used by pathfinding operations.
 *
 * `x` maps to the grid column and `y` maps to the grid row.
 */
export type GridCell = Vector2;
