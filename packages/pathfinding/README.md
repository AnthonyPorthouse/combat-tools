# @combat-tools/pathfinding

Grid-based pathfinding utilities for Combat Tools.

This package provides A\* helpers for token movement, nearest-valid-cell resolution, and occupied-cell grid generation.

`GridCell` is exposed as a semantic alias of `Vector2`, where `x` maps to column and `y` maps to row.
